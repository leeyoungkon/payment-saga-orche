apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-saga-orche
  namespace: kafka
  labels:
    app: payment-saga-orche
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-saga-orche
  template:
    metadata:
      labels:
        app: payment-saga-orche
    spec:
      containers:
        - name: payment-saga-orche
          image: yklee2002/payment-saga-orche:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              value: my-cluster-kafka-bootstrap.kafka:9092
---
apiVersion: v1
kind: Service
metadata:
  name: payment-saga-orche-service
  namespace: kafka
  labels:
    app: payment-saga-orche
spec:
  type: NodePort
  selector:
    app: payment-saga-orche
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30303
